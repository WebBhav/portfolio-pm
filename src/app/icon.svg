<svg width="100" height="100" viewBox="0 0 100 100" xmlns="http://www.w3.org/2000/svg">
  <!-- Background Circle (optional, can adjust color or remove) -->
  <circle cx="50" cy="50" r="48" fill="#303030"/>

  <!-- Initials 'VS' -->
  <text
    x="50%"
    y="50%"
    dominant-baseline="central"
    text-anchor="middle"
    font-family="Arial, sans-serif"
    font-size="50"
    font-weight="bold"
    fill="#00BCD4" /* Teal color for initials */
  >
    VS
  </text>
</svg>
